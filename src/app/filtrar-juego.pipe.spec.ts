import { FiltrarJuegoPipe } from './filtrar-juego.pipe';

describe('FiltrarJuegoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarJuegoPipe();
    expect(pipe).toBeTruthy();
  });
});
